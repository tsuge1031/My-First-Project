{
  "name": "golf-inventory-cloudflare",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply golf_inventory --local",
    "db:migrate:remote": "wrangler d1 migrations apply golf_inventory --remote"
  },
  "dependencies": {
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250419.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.16.0"
  }
}
